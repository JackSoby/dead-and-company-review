import React from 'react';
import ReactDOM from 'react-dom';
import DeadAndCompanyReview from '../react/src/DeadAndCompanyReview';

document.addEventListener('DOMContentLoaded', () => {
  
  ReactDOM.render(<DeadAndCompanyReview />, document.getElementById('app'));
})
